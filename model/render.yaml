services:
  - type: web
    name: bu-dining-api
    runtime: python
    plan: free
    rootDir: model
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn 03_api:app --host 0.0.0.0 --port $PORT
    autoDeploy: true